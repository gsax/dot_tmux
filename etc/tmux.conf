#################
## keybindings ##
#################

# use C-s instead of C-b
set -g prefix C-s
unbind C-b
bind C-s send-prefix
bind g last-window
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
bind s split -v
bind v split -h

# use vi mode as default
set -g mode-keys vi

##################
## misc options ##
##################

# don't start as login shell
set -g default-command zsh

# 256 colors support
set -g default-terminal "tmux-256color"

# 24bit color support
set -ga terminal-overrides ",*256col*:Tc"

# bell action
set -g bell-action any

# number of lines in history
set -g history-limit 100000

# automatically renumber windows
set -g renumber-windows on

# set escape timeout
set -sg escape-time 0

# message line
set -g message-style "fg=green, bg=black, bright"

# pane borders
set -g pane-border-style "fg=red"
set -g pane-active-border-style "fg=blue"

# time after prefix
set -g repeat-time 100

# statusbar options
set -g status on

# default statusbar colors
set -g status-fg blue
set -g status-bg default

# statusbar left
set -g status-left-style "fg=blue, bg=default, dim"

# statusbar right
set -g status-right ""
set -g status-right-style "fg=blue, bg=default, dim"

# set window titles
set -g set-titles on
set -g set-titles-string "#W"

####################
## window options ##
####################

# activate altscreen
set-window-option -g alternate-screen on

# automatic rename of panes
set-window-option -g automatic-rename on

# clock options
set-window-option -g clock-mode-colour green
set-window-option -g clock-mode-style 24

# default window title colors
set-window-option -g window-status-format "#F#I:#W"
set-window-option -g window-status-style "fg=blue, bg=default, dim"

# alerted window title colors
set-window-option -g window-status-bell-style "fg=yellow, bg=red, bright"

set-window-option -g window-status-activity-style "fg=yellow, bg=red, bright"

# active window title colors
set-window-option -g window-status-current-format "#F#I:#W"
set-window-option -g window-status-current-style "fg=green, bg=default"

##################
## user scripts ##
##################

# copy tmux paste buffer to CLIPBOARD
bind C-c run "tmux show-buffer | xclip -i -selection clipboard"

# copy CLIPBOARD to tmux paste buffer and paste tmux paste buffer
bind C-v run "tmux set-buffer \"$(xclip -o -selection clipboard)\"; tmux paste-buffer"

# browse URL's
bind-key u capture-pane -J -S - \; \
	save-buffer /tmp/tmux-buffer \; \
	delete-buffer \; \
	split-window -l 10 'url_filter.sh /tmp/tmux-buffer'

# clear buffer
bind y clear-history

